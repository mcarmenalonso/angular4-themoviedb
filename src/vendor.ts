/**
 * All third party vendors that is being used through out the application
 * NOTE: Do not use any vendor which is being used for only single component
 * For vendor files that is being used in only few components use lazy loading.
 */

// Angular
import "@angular/platform-browser";
import "@angular/platform-browser-dynamic";
import "@angular/core";
import "@angular/common";
import "@angular/router";

import "rxjs";

// Other vendors for example boostrap
// css
import "bootstrap/dist/css/bootstrap.min.css";
import "font-awesome/css/font-awesome.min.css";
// js
import "bootstrap/dist/js/bootstrap.min.js";
